# 三种观点权衡与实现方案

> 本文记录实施前的权衡与规划。当前实际完成的功能、验证结果和边界见 [应用说明](./decision-desk/README.md)。

日期：2026-09-05。性质：基于 ZIP 原始材料形成的独立建议，供团队直接据此开工；不代表三位参与者已经共同确认，也不覆盖原来的需求记录。

**建议实现一个“决策对账台”：以 Y 的执行前拦停与向前纠正为控制主线，以 S 的双栏分层时间线作为主界面，吸收原方案对独立审查和真实验证的要求。首版只接一个执行 Agent，异源模型负责对账；全局回滚与多个执行 Agent 的协调放到后续。**

产品要跑通的闭环是：人明确需求 → 看见 Agent 额外作出的重要选择 → 在冲突动作执行前作决定 → Agent 按新约束继续或修补 → 用实际结果记录这次干预的效果。

## 1. 阅读范围与判断依据

唯一项目来源是 `build-with-care-main.zip`（用户提供的原始仓库拷贝）。ZIP 已单独解压到 本次独立审阅目录，未读取已有的 `build-with-care-fresh` 文件夹。

- ZIP 内共 19 个文件、133,216 字节；16 个 Markdown 文档全部读完，包含隐藏目录中的记录、访谈技能文件，以及空白讨论文档。
- 另逐行阅读唯一的 POC 测试代码，并检查 `.gitignore` 与空占位文件。
- ZIP 中没有应用实现、依赖清单、锁文件、dsh 源码、`mock-adapter.ts` 或可独立运行的工程。它主要是一份需求与研究档案，附带测试片段。
- 文档提到的二轮总报告 `docs/research-2.md`、引用核实报告、早期所称的 `05-deepseek-harness.md` 不在 ZIP 中。不能把它们当作已完成的证据。
- 本次补查了 dsh 官方接口源码与 LangGraph 官方文档，用于核对实现边界；这些属于补充核查，不能替代 ZIP 当时所用版本的实际运行验证。

SHA-256：`A1CCFB31353B7F5687F578C52545EDA719060C737A737EDEB5C8440654F37346`。

证据使用顺序：赛事要求用于确定目标，三份需求记录用于辨认各方偏好，POC 代码用于判断实际覆盖了什么，研究报告用于提供线索。多份 AI 调研重复同一个结论，不等于获得了多份独立验证。

## 2. 三种观点分别在解决什么

这里的“三种观点”指三个参与者的需求方案，不能把它们与技术报告里的 A/B/C 三条接入路线混为一谈。

| 维度 | P：pchan2026 原方案 | S：darksoda 方案 | Y：Yonack1 修订方案 |
|---|---|---|---|
| 核心对象 | Agent 替人作出的决定及其状态 | 人的要求与 Agent 行为的逐步对账 | 可供人干预的重要决策卡 |
| 对照基线 | 对话原文，不强制开工计划 | 人类指令及其过程记录 | 简短 spec，经人确认后执行 |
| 观察者 | 独立判官 Agent | 明确要求异源模型记录、对账 | 卡片抽取机制；未像 S 那样明确限定异源 |
| 默认打扰 | 蓝卡不阻塞，约束冲突立即阻塞 | 所有偏差只标记，继续执行 | 蓝卡静默，红卡阻塞 |
| 纠正方式 | 新约束向前生效 | 全局回滚重做，原时间线暂存 | 向前注入，也可紧急叫停 |
| 出错处理 | 普通错误自修，连续失败再求助 | 主要依靠发现后回滚 | 强调约束冲突与停止，验证语义较少 |
| 成果证明 | 一份会话报告 | 分层时间线、分支历史与一页摘要 | 时间线回放，省去独立统计页 |
| 执行规模 | 不做多 Agent 编排 | 一个人指挥多个执行 Agent | 首先做单执行 Agent 的现场闭环 |
| 最新底座态度 | 倾向 dsh，尚挂起 | dsh 外部工作台、事件流与 Git 打点 | dsh 插件；此前的 Claude SDK 方案已被修订 |

来源：[P 需求记录](./docs/requirements-alignment.md)、[S 需求记录](./docs/requirements-alignment-S.md)、[Y 需求记录](./docs/requirements-alignment-Y.md)。S 文档中的“重置”表明 darksoda 重新表达了自己的方案；材料不足以证明其他人的方案因此共同作废。

### P 的强项与代价

P 最关注“平时少管，但我看到的状态要可信”。不强制计划降低了进入门槛，验证成功才标绿也很有价值；把普通运行错误与违反人类约束分开，避免每次测试失败都叫人。

代价在判定基线：对话长了会有补充、撤销、条件限制，仅把全文交给判官，容易混淆哪个要求仍然有效。同时，蓝色表达“谁决定的”，绿色表达“是否验证过”，把两者设计成互相替换的状态，会丢失决策来源。

**取舍：保留独立审查、验证证据与错误升级；增加简短的有效约束清单，拆开来源与结果状态。**

### S 的强项与代价

S 最关注“看清过程，能指出具体哪一步不对，并保留另一条做法”。双栏记录更接近人的理解方式，粗粒度展示、细粒度展开也适合长任务。它对已完成工作发生根本性偏差时的恢复需求，比纯向前注入考虑得更充分。

但“只标记 + 全局回滚”的组合把纠错成本压到了事后：冲突发生后仍继续执行，发现得越晚，受牵连的后续工作越多。旧分支保留了成果，却不保证成果能自动接回新分支。对多个执行 Agent，还要统一文件快照、各自会话位置、运行中命令和约束版本；一个 Agent 的 `turn/end` 并不意味着其他 Agent 停止写文件。

**取舍：采用双栏分层记录；首版不采用冲突也继续执行、不做多 Agent 全局回滚。** 这项取舍依据是交付规模和状态一致性成本，而不是“竞品做过，所以不能做”。赛事说明明确允许与已有产品重叠。

### Y 的强项与代价

Y 最关注“把一个能证明人确实管得住 Agent 的闭环做出来”。轻量 spec 给判官稳定参照；只让冲突打断人；拒绝、注入和叫停组成清楚的控制链；已有 POC 给了接口层面的起点。因此最适合做首版骨架。

代价是容易过度乐观：确认 spec 也可能变成随手点同意；注入文本不等于模型已遵从；向前纠正也可能需要修改旧代码；复用 dsh UI 仍需解决事件桥接和审批状态。只做几张卡片，则容易失去 S 关心的完整上下文。

**取舍：采用控制链，收紧 spec 长度，加上双栏证据与结果验证；不沿用“引擎风险清零”和“前向纠正不损耗工作”的表述。**

### 权衡后的优先顺序

优先保证：①人能在冲突动作前干预；②两分钟内看得懂；③在剩余时间内真实跑通；④记录足以说明人的判断；⑤后续可扩展。按这个顺序，Y 适合作为控制骨架，S 的记录形态最值得吸收，P 的验证标准应成为验收底线。

这个组合保留了各方案中解决同一问题的有效部分，并明确拒绝了互相冲突的行为语义：**已识别的明确约束冲突必须停；已完成部分通过新增修补任务纠正；历史保留，但首版不提供历史分叉执行。**

## 3. 仓库里哪些结论需要修正

| 原材料的判断 | 本次审阅结论 | 对实现的影响 |
|---|---|---|
| POC 证明模型收到理由后改道 | 测试中的下一次输出预先设为 Postgres；只能说明理由进入了下一次请求、预定工具调用可以继续 | 必须做真实模型的拒绝后重规划验证 |
| 已有红蓝卡抽取验证 | POC 用字符串是否含 `sqlite` 判红，其他文件名判蓝 | 判官质量、重要决策筛选与误报仍是核心开发工作 |
| 四个引擎机制风险清零 | ZIP 只有三段自写测试；Slots 的 42 项测试和拦截的 23 项测试是报告所述，未附完整运行记录 | 可沿用测试意图，不能称本次已复现；补测真实插件与异常路径 |
| 写几个 Git 命令即可完成全局回滚 | 原生 fork 有会话边界限制；文件与多会话的一致快照仍要另做 | 不将回滚列为首版“顺手实现”的功能 |
| S 与 Y 对能否改写工具参数表述不同 | S 总览宽泛称流水线可变换调用；Y 明确指出入参不可直接改写，当前官方源码也把调用身份与参数视为不可变 | 使用拒绝旧调用，再由 Agent 提出新调用 |
| 向前翻案完全空白，回滚会丢掉旧时间线 | 现有官方接口已经支持会中引导；LangGraph fork 明确保留原历史 | 差异化应强调自动对账、人的干预与可核查结果的组合体验 |
| 没操作就没贡献，未翻案就算放过 | 没操作可能是没看到；人的需求、判断和验收也有贡献 | 未操作标为“未审阅”，统计显式行为，不给人打能力分 |
| 记录即可证明“一年后人变强” | 时间线能提供复盘材料，长期成长仍需实际评估 | 首版增加解释与简短复盘，不宣称已经证明长期能力提升 |

POC 关键位置：[预设模型输出](./poc/decision-stream-poc.spec.ts#L65)、[字符串判色](./poc/decision-stream-poc.spec.ts#L84)、[报告的风险结论](./docs/test-dsh-poc-Y.md#L27)。

外部复核确认：dsh 当前 `pre-execute` 可异步作出执行决定，挂起方需要自行处理取消信号；不能从“存在钩子”推导出整个插件会自动可靠停止。[官方工具源码](https://raw.githubusercontent.com/deepseek-ai/deepseek-harness/master/packages/core/tools/src/index.ts)

dsh 的普通 fork 要求选中的日志前缀结束于完整 turn 之外；这与界面上任意语义步骤并不一一对应。[官方会话文档](https://raw.githubusercontent.com/deepseek-ai/deepseek-harness/master/docs/subsystems/session.md)

LangGraph 官方说明 fork 保留原历史，之后的节点、模型调用和 API 请求会重新执行。这也说明“保留历史”本身不足以构成差异化。[官方 time-travel 文档](https://docs.langchain.com/oss/python/langgraph/use-time-travel)

调研中的模型通过率、400 行阈值、市场空白、公司关停原因等，未在本次逐一核实，不作为路线决策或路演的定量支柱。两份 dsh 调研中的 Star 数也不一致，产品定位无需依赖这一数字。

## 4. 首版具体做成什么

### 4.1 一个工作台，四个清楚的区域

顶部：用户的原始需求、当前有效的 3–5 条约束、运行状态和始终可用的“停止”。

中间：S 式双栏时间线，左侧是相关的人类要求，右侧是 Agent 的拟执行动作或实际结果。每个语义步骤可以展开原始工具调用、文件变化和验证证据。拟执行必须标为“准备做”，拿到工具结果后才能写“已执行”。

侧栏：重要决策列表。未指定的重要选择静默进入；明确约束冲突进入待处理状态，只显示一个当前待处理入口，避免连续弹窗。

底部：本次干预摘要。列出人提出的要求、确认的选择、作出的纠正，以及哪些纠正已有结果证据；可以点回对应步骤。直接由同一时间线数据生成，避免另造统计系统。

### 4.2 开始任务：确认轻量约束，不强制长计划

人输入一句需求。系统只提取原文已经明确的约束，生成一张可编辑的短清单；保留每条约束对应的原话。Agent 补充的方案要标为“建议”，没有指定的事项保持“未指定”。人确认后开始执行。

例如：“做个中文活动报名页，只在本机演示，不要登录。”确认清单应包含中文、本地演示、免登录；存储方案若无法从原话明确推出，就保留为未指定。不得在扩写时默默加上云数据库或固定某个框架。

这选择了 Y 的一次确认，吸收 P 对低门槛的要求。清单只描述有影响的要求，不列文件名、变量名等执行细节。以后用户补充要求，形成新的约束版本；旧版本与原话都保留。

### 4.3 决策分类与展示状态分开

| 情况 | 展示 | 执行行为 |
|---|---|---|
| 与约束一致的普通执行 | 默认折叠进过程日志 | 自动继续 |
| 未指定的重要选择，如存储、登录、外部服务 | 蓝色“Agent 提议”标签 | 检查通过后继续，用户可随时纠正 |
| 拟执行动作与明确有效约束冲突 | 红色“约束冲突”，显示原话和证据 | 工具执行前暂停，等待处理 |
| 单次测试或构建失败 | “执行失败”状态 | Agent 在原约束内修复；同一问题连续 3 次失败再求助 |
| 证据不足或审查服务异常 | “待核对／审查失败”，不冒充业务冲突 | 受控写操作保持暂停；允许人工查看后处理 |
| 对应检查真正通过 | 绿色“此项检查通过”，附检查项和时间 | 不抹掉原先的决策来源、冲突与干预历史 |

一次测试通过不代表整个决策正确。证据必须绑定具体文件版本或内容摘要，相关内容再变化后将检查标为“需要重新验证”。用户只看到过卡片不能算同意；可以单独点“认可”，也可以不处理。

### 4.4 人可以怎样介入

- **按原要求改正**：拒绝当前冲突调用，解释冲突，让 Agent 重新提出动作；原约束不变。
- **改成另一种做法**：选择建议项或输入一句话，保存新约束，再让 Agent 继续。
- **仅本次允许**：显式允许这一条具体调用，记录例外；不默默解除整个约束。
- **停止任务**：立即关闭新的执行入口，取消当前活动，界面显示实际停止进度。

建议选项由异源审查模型生成，用户始终可以自己写；第一版每张卡最多两个建议，避免花时间设计复杂选择器。

## 5. 纠正语义：明确何时生效、如何处理旧成果

“向前生效”定义为：**从服务端接受该次纠正、更新约束版本后，尚未开始执行的受控动作都按新版本重新检查。历史事件不改写；若旧成果受影响，追加修补步骤。**

不能只调用一次注入接口就显示“已纠正”。当前 dsh 的 `inject` 会等待后续步骤，不唤醒空闲 Agent，也可能错过已经领取上下文的那一次请求。[官方 Agent 接口](https://raw.githubusercontent.com/deepseek-ai/deepseek-harness/master/packages/core/agent/src/runtime-types.ts)

服务端执行顺序：

1. 接收纠正时先短暂关闭该会话的新写操作入口，校验卡片和约束版本。
2. 保存人的操作、原话、目标决策及新约束版本，成功后才向界面确认收到。
3. 拒绝受影响的待执行旧调用；让其他排队调用重新校验，不能沿用旧判断直接执行。
4. 将纠正内容送进 Agent 下一次可用上下文；Agent 已空闲时使用能启动后续工作的通道。
5. 记录“已收到 → 已进入上下文 → 已产生后续动作 → 对应验证通过／仍未解决”。

已经开始的命令可能完成或部分执行。界面必须显示其真实结果，停止后检查受影响文件；不能承诺按键瞬间撤销一切。首版限制为本地演示项目，不接发送、支付、部署等外部写操作，降低需要补偿的范围。

例子：Agent 已用本地存储写完表单，人后来要求只保存到内存。系统保留旧记录，给当前代码追加一次存储层修补，并重新验证刷新后的行为。页面其他部分可以保留，但修补成本真实存在。“不回滚历史”不意味着“永远不改旧代码”。

如果修补明显不划算，用户可停止并重新开一个任务；首版不伪装成具有任意历史分叉能力。

## 6. 技术实现：dsh 薄插件 + 一套对账逻辑 + Web 面板

### 6.1 路线与范围

首选 TypeScript，基于 dsh 开发外部插件，复用其 Agent 运行和会话界面。审查器是一个无工具权限的异源模型调用模块，负责结构化分类和解释；不再创建一个能自由调工具、循环推理的“判官团队”。

这是对 P、S“独立观察”的工程化收敛：执行模型不能决定自己是否被审查；审查模型必须引用人的约束与真实动作。异源有助于减少同一种遗漏反复出现，但不保证正确，仍需样本评估。

当前官方仓库仍标明 developer preview。因此要锁定实际验证过的 dsh 提交 SHA、安装版本与依赖锁文件。ZIP 所写 `0.1.3-alpha.1` 是候选版本，原测试没有记录具体 SHA；不能用最新 master 冒充复现了旧测试。[dsh 官方仓库](https://github.com/deepseek-ai/deepseek-harness)

在第一小时验证真实模型连接、工具拦截、拒绝理由与取消。原报告在 Debian 下跳过原生模块的办法只覆盖了它的测试环境，不推导为当前 Windows 或完整 Web 应用一定可运行。

### 6.2 五个模块及其边界

| 模块 | 职责 | 关键输入、输出 |
|---|---|---|
| dsh 接入插件 | 捕获执行前动作、执行结果、消息；控制拒绝、注入和停止 | 原始事件 → 统一事件；裁决 → dsh 接口 |
| 约束与执行控制 | 保存有效约束、待执行动作和人工处理；控制何时允许执行 | 带版本的约束、准确工具参数 → 一次执行决定 |
| 异源审查器 | 判断普通执行、重要选择、约束冲突或证据不足；生成中文说明 | 约束原话、参数、相关文件片段 → 有证据引用的结构化结论 |
| 时间线与证据存储 | 保留原始事件和衍生记录，聚合语义步骤，关联验证 | 追加日志 → 可重建的步骤、决策、干预摘要 |
| Web 面板 | 短约束确认、双栏时间线、处理卡片、停止、复盘 | 状态快照和增量事件；人的显式操作 |

首版将控制、审查和存储放在同一 Host 插件进程，避免审批在多个服务间来回转发。执行模型与审查模型仍分别配置。面板只传人的操作，不持有模型密钥，也不自行决定工具能否继续。

先让面板能作为独立网页连接 Host，再尝试以 dsh Slots 嵌入。使用自有路由与事件流，不依赖未核实的自定义事件自动桥接。Slots 对接限时 45 分钟，失败就保留独立页面并排演示，复用同一组件和协议。

本地服务绑定回环地址，并沿用会话认证或加启动时生成的令牌；裁决请求必须校验会话归属，避免别的页面误处理当前卡片。

### 6.3 执行前与执行后，两次各做什么

执行前：

1. 捕获原始工具名、完整参数、会话和调用标识，保存“准备执行”事件。
2. 对已知只读、无副作用的基础工具走快速规则；写文件、修改依赖、执行命令等受控动作进入检查。不能仅凭命令描述把任意 shell 判成只读。
3. 将当前有效约束、相关原话、拟执行动作及必要上下文交给异源模型，校验返回结构和引用是否存在。
4. 普通执行自动继续；重要未指定选择生成蓝卡后继续；明确冲突生成红卡并挂起；审查异常保持待处理。
5. 真正进入工具执行前再核对约束版本和停止标记。发生变化就重检。

执行后：

1. 用工具结果和实际文件变化更新“做了什么”；模型口头说“完成”只算一条消息。
2. 把多次有关联的工具调用合成一个语义步骤，保留展开入口。
3. 把测试、构建或行为检查与对应产物版本关联。
4. 人的纠正与后续动作、验证建立链接；没有证据就保持“效果待验证”。

首版默认只允许一个执行 Agent，并串行调度受控写操作。这样可以明确“人修改约束”和“工具开始执行”的先后关系，不在原型里承担并行写入的一致性成本。

低打扰指减少向人提问，并不意味着完全没有检查延迟。只读事件不逐条调用模型；卡片说明、备选项和报告摘要可异步补齐，但受控动作所需的冲突判定必须在执行前完成。

### 6.4 最小数据契约

| 实体 | 必需字段 | 约束 |
|---|---|---|
| Event | id、runId、agentId、sessionId、sourceSeq、receivedSeq、type、time、payloadRef | 原始记录只追加；来源序号用于去重，本地序号只表示接收顺序 |
| Constraint | id、revision、text、sourceMessageId、scope、status、supersedes | 系统建议与人确认的要求分开；不能靠模型总结覆盖原话 |
| Step | id、eventIds、title、relatedConstraintIds、status | 语义分组不改变原始事件；不等于可恢复快照 |
| Decision | id、stepId、topic、scope、origin、compliance、evidenceRefs、options | 来源、符合性与执行状态独立保存 |
| Gate | id、toolCallId、toolName、argsHash、constraintRevision、status、expiresAt | 人的允许只对应准确的一次调用；参数或版本变化就失效 |
| Intervention | id、requestId、decisionId、gateId、actor、action、text、fromRevision、toRevision | 幂等处理重复点击，保留人的原始输入 |
| Verification | id、decisionId、checkName、artifactHash、result、evidenceRef | 通过只覆盖该检查和产物版本，不自动扩为整体正确 |

语义去重与执行授权必须分开。比如“使用本地存储”可以只显示一张持续更新的卡，但之后每一次文件修改仍需检查；不能因为已经展示过这张卡，就永久放行同类调用。

建议最小接口：获取会话快照、从指定事件序号继续订阅、提交约束确认、提交卡片操作、停止运行、获取本次摘要。提交操作携带 `requestId`、卡片版本和约束版本；重复请求返回同一结果，过期操作返回明确的冲突状态。

使用插件自己维护的单写入者 JSONL 保存事件与裁决，界面数据由它重建；同一裁决在持久化成功之前不能释放工具。优先复用 dsh 原始日志引用，避免复制所有大段输出。首版不承诺崩溃后的任务自动续跑；重启后能恢复查看，未决审批标为中断，需要重新发起受控动作。

### 6.5 挂起、异常与停止必须单独处理

- 模型检查默认 8 秒超时，可配置；超时不放行写操作，显示“审查服务未完成”，允许重试或人工处理。
- 人工待处理默认保留 10 分钟；过期后拒绝该调用并暂停会话，不连续重试刷卡。
- 挂起的 Promise 同时监听取消信号；注册前检查信号是否已取消，处理后移除监听器。
- 点击停止先设置服务端停止标记，再取消 Agent 和未完成的检查；所有 pending gate 都收敛为拒绝。
- 被拒绝后若 Agent 连续提交同一违规动作，按“约束冲突重试”聚合并暂停，不能混入普通测试失败的 3 次自修计数。
- 若已有命令正在执行，显示“正在停止”；等待实际结束再显示“已停止”。取消消息入队不等于进程已结束。
- 浏览器刷新或断线不表示允许；恢复后读取服务端状态，避免重复放行。

## 7. MVP 与后续能力的明确分界

| 优先级 | 实现内容 | 完成标准 |
|---|---|---|
| P0：必须完成 | 轻量约束确认、单执行 Agent、异源对账、蓝卡静默、红卡阻断、自由文本纠正、停止、真实结果关联、静态时间线与短摘要 | 能从需求一路走到干预后的产物验证 |
| P1：P0 完成后 | 两个建议选项、时间线展开细节、检查徽标、Slots 嵌入、简短复盘输入 | 改善理解与展示，不引入新执行语义 |
| P2：赛后 | 多执行 Agent 的统一观察、作用域约束、隔离工作区、完整分支恢复、跨会话复盘 | 分别验证新增的状态管理与恢复语义 |

首版中的“一个执行 Agent + 一个审查模型”应如实这样描述，不能宣称已经实现了 S 的多个执行 Agent 协同。赛题四个方向是参考，单执行 Agent 的监督工具仍能回应“人跟不上 Agent”的命题。

多 Agent 后续首先做统一观察，使用各会话序号、任务关联和消息关系表达因果；不能把收到事件的顺序当作全局发生顺序。再考虑控制与写入隔离。

真正的分支恢复至少需要：等待所有相关执行器停止、保存完整的多会话位置与约束版本、将它们和文件快照绑定、在新工作区恢复、处理重跑外部副作用、验证新旧分支切换。首版只预留 `runId/agentId/sessionId` 等标识，不提前搭完整恢复框架。

## 8. 验收计划：先证明闭环，再打磨界面

以下均为开发验收要求；本次任务只完成阅读与规划，没有运行仓库所称的 POC，也没有执行下列测试。

### 8.1 第一小时的技术通路检查

在锁定版本上，以现场可用模型完成一次真实读写任务。插件拿到完整写入参数；挂起时目标文件未变化；拒绝理由进入下一次真实模型请求；继续后出现实际产物；停止能结束待处理状态。记录版本、网络端点类型、事件日志与结果。

SDK 或 UI 组件的独立测试不替代这一步。若旧 POC 要复现，先补齐对应 dsh 源码、缺失的 MockAdapter 与依赖，再将三段测试作为通路回归参考。

### 8.2 审查质量：最多 45 分钟的小样本筛选

准备 24 条人工标注案例，覆盖明确冲突、重要未指定选择、普通执行和证据不足，各含表述变化；12 条用于调整，另 12 条保留验证。最多比较两个现场已可用的异源模型，避免花半天选型。

不使用单个总体“准确率”遮住问题：分别记录冲突漏报、普通执行误报、重要决策是否浮出、证据引用是否正确以及检查耗时。保留样本中的明确冲突要求全部识别；若达不到，收窄首版支持的约束类型，并在界面说明范围。

这个样本量只能筛掉明显不合格的配置，不能证明生产环境的可靠率。字符串规则适合核验明确路径或依赖禁用；语义冲突仍需真实审查模型评估，不能用固定关键词命中冒充理解。

### 8.3 必测的端到端情形

| 场景 | 应观察到的结果 |
|---|---|
| 普通读取与普通执行 | 不要求人逐条确认，原始过程可查 |
| 未指定的重要存储选择 | 有一张可理解的蓝卡，不因文件名变化生成大量卡片 |
| 明确禁止某依赖，Agent 提议加入 | 提案留下记录；实际依赖文件在人工处理前保持原样 |
| 红卡上提交纠正 | 旧调用被拒，后续真实模型收到纠正，产物满足新要求 |
| 蓝卡对应工作已完成后再纠正 | 增加修补记录；旧历史保留；相关产物被重新验证 |
| 同一批有多个待执行动作时修改约束 | 后续未执行动作重检，不漏过旧版本调用 |
| 审查超时、页面断线、重复点击 | 不意外放行、不产生重复干预；重连能恢复状态 |
| 挂起时停止、信号已提前取消 | 工具不执行，gate 结束，Agent 进入停止状态 |
| 命令执行中停止 | 不再开始新动作，正确报告已经发生的影响 |
| 检查通过后相关文件又变化 | 旧检查显示需要重新验证，不继续把新版本标绿 |
| 用户从未处理蓝卡 | 摘要显示“未审阅”，不统计为“人批准” |
| 同一错误连续 3 次修复失败 | 聚合成一次求助，不无限循环 |

可以用可控工具提案确定性地检验门禁，但这不证明真实模型行为。还需完成三次真实模型的完整“需求—卡片—人工纠正—产物验证”流程，保留日志与录屏。若未自然遇到真实红卡，应如实说明，不能把模拟冲突包装为真实模型出错。

### 8.4 用户体验目标

找一位未参与实现的同学，用 30 秒说明当前卡片“人要求什么、Agent 想做什么、需要我决定什么”。若说不清，优先改卡片文案和证据位置。

一个 10–15 分钟演示任务，目标是 3–6 个重要决策、最多约 3 次主动打扰；这些是设计目标，不是通过隐藏真实冲突达标的配额。审查响应目标为常见情况下 3 秒左右，超时路径仍必须可理解。

## 9. 按实际剩余时间推进

按 ZIP 的赛事说明，冻结时间为 9 月 6 日 12:30（北京时间）。本次核对时间约为 9 月 5 日 14:05，剩余约 22 小时 25 分钟。原文的“43 小时”是完整赛程，9 月 5 日 14:00 的旧里程碑已经过去，不能照抄为剩余排期。

以下按 Y 记录中“两条开发线”的人力假设估算。实际开发起点若晚于下午，应压缩功能，不挤掉明早验证。未读取旧解压文件夹，因此不对团队其他位置已经完成多少作假设。

| 时间窗口 | 运行与控制线 | 面板与证据线 | 交付门槛 |
|---|---|---|---|
| 开工后 0–1 小时 | 锁版本，验证真实连接、挂起、拒绝、取消 | 约定实体与操作协议，做短约束与卡片骨架 | 明确当前底座可用；失败当场处理 |
| 第 1–3 小时，约至 17:30 | 接异源审查、人工纠正、约束版本；完成小样本评估 | 接真实事件，完成双栏和卡片处理 | 第一条真实端到端闭环 |
| 第 3–6 小时，约至 20:30 | 完成停止、过期操作、超时、重复请求与修补检查 | 时间线展开、证据链接、短摘要 | P0 核心功能齐备 |
| 第 6–8 小时，约至 22:30 | 修复集成问题，保存测试日志 | 联调、观察同学理解情况、简化文案 | 功能冻结，只留影响演示的修复 |
| 9/6 09:00–11:00 | 三次真实流程与异常复查 | 录屏、两分钟彩排、一页说明 | 有可复现结果与故障备用展示 |
| 9/6 11:00–12:00 | 整理启动说明与依赖版本，干净启动验证 | 检查代码、README、说明和演示素材 | 12:00 内准备好提交，留 30 分钟余量 |

如果只有一位开发者，直接采用独立网页、自由文本纠正、静态双栏列表与模板摘要；取消 Slots、建议选项和播放动画。仍保留阻断、纠正、停止和验证。

### 降级顺序

1. 先砍时间线播放动画与复杂统计，保留静态证据列表。
2. 再砍模型生成的备选项，保留一句话纠正。
3. Slots 卡住则用独立页面；不为嵌入耽误闭环。
4. 审查效果差则收窄支持范围，例如只做存储、依赖和登录三类重要决策；在支持范围内继续真实判断。
5. dsh 首小时若因安装或接口无法运行，立即决定是否采用最小 TypeScript 工具循环：一次模型请求 → 工具提案 → 同一个 gate → 执行 → 结果回传，工具仅限项目读、写、修改和预定验证；沿用同一面板及协议。替代循环须在随后 1 小时通过同一通路验收，否则记录交付风险，不能继续承诺完整范围。

第 5 项是应急实现路径，并无 ZIP 内现成代码可直接复用。**不把 Claude CLI hooks 的长超时当作当然可靠的保底**：原材料自己指出超时可能回落到正常权限流程。换底座也必须通过“人未处理时受控动作不能执行”的同一验收条件。

如果到当晚仍没有真实闭环，停止增加 UI 和报告细节，集中于一项明确约束、一种工具与一句话纠正。不能降级成只观察、不干预的看板，却仍声称实现了本方案。

## 10. 两分钟展示与人的成长如何落地

演示任务选“免登录的中文活动报名页”，优先使用纯本地、容易观察变化的小应用。把存储选择或名额规则作为一个真实决策点，避免演示依赖外部服务注册和长时间构建。

| 时长 | 展示内容 |
|---|---|
| 0–15 秒 | 用户需求与短约束，指出一个没有指定的选择 |
| 15–40 秒 | Agent 的真实工作流入双栏，重要选择出现为卡片 |
| 40–85 秒 | 用户用一句话纠正，展示新约束、后续动作与实际页面行为变化 |
| 85–105 秒 | 展示约束冲突如何阻断及停止入口；优先用真实冲突记录，回放要明确标识 |
| 105–120 秒 | 摘要点回人的判断、改变的产物和验证证据 |

不要依靠“预埋需求文案一定能让模型犯错”保证红卡。模型遵从需求时，本来就不该出红卡。准备清楚标识的真实录屏；若用故障注入或固定提案检验门禁，也应标明演示模式。现场模型改向、卡片判定和历史回放的来源必须讲清楚。

回应“人变强”的首版做法：纠正后展示这项选择改变了什么，以及如何验证；收尾可让人用一句话写“下次我会先明确什么”。复盘问题可选，不增加执行审批。系统记录的是能够检查的参与过程，长期学习效果留给后续用户研究。

提交物仍按赛事要求准备：可运行 Demo、代码及运行 README、一页项目说明、两分钟路演。README 必须说明模型配置、启动顺序、支持的动作范围和如何复现一条真实干预记录。

## 附录：全部文档阅读清单

下面的路径均相对于本次新解压的 `build-with-care-main`，每一项都已全文阅读；清单用于核对覆盖范围，不表示照单采纳原结论。

| 文档 | 内容与本次用途 |
|---|---|
| `docs/info.md` | 赛题原文、边界、资源与提交要求；确定目标和冻结时间 |
| `docs/track3-breakdown.md` | 赛题解释与早期方向映射；与原文对照，区分硬要求和作者建议 |
| `research.md` | 一轮痛点、竞品与决策流提案；吸收问题定义，收紧市场空白和贡献推论 |
| `docs/requirements-alignment.md` | P 观点原记录；独立判官、对话基线、验证状态、自修与报告 |
| `docs/requirements-alignment-S.md` | S 观点原记录；双栏、多 Agent、非阻塞对账与全局回滚 |
| `docs/requirements-alignment-Y.md` | Y 观点及 D5-R 技术修订；轻量 spec、拦停、纠正与插件路线 |
| `docs/plan-43h-Y.md` | 原倒排、接口任务、降级顺序；依据当前时间重排，修正保底可靠性 |
| `docs/research-2-raw/01-technical.md` | Claude hooks/SDK 技术路径与 fail-open 风险；作为备选路线的风险依据 |
| `docs/research-2-raw/02-competitive.md` | 二轮竞品复查；不沿用“向前翻案完全空白”的绝对表述 |
| `docs/research-2-raw/04-chinese-community.md` | 中文开发者反馈与土办法；支持意图、维护和参与记录的产品动机 |
| `docs/research-2-raw/05-dsh-feasibility-S.md` | dsh 事件、注入、会话分叉、工具流水线；重点检查回滚和粒度限制 |
| `docs/research-dsh-Y.md` | dsh 插件、Slots、入参限制；用于确定薄插件与面板降级方式 |
| `docs/test-dsh-poc-Y.md` | POC 测试报告和环境限制；与测试代码对照，识别证据边界 |
| `.agent/notes/2026-09-04-research-round-2.md` | 二轮工作记录，表明引用核实尚未返回、总报告未完成 |
| `.agent/skills/grill-me/SKILL.md` | 访谈方法说明；作为档案读取，本次未启动访谈流程 |
| `docs/pre-discussion.md` | 空白文档；无可用于推断需求的内容 |

其他三个文件：`poc/decision-stream-poc.spec.ts` 已逐行阅读；`.gitignore` 已检查；`.agent/notes/.gitkeep` 为空。全部文件的字节数与散列已校验，原始阅读清单保留在本地审阅目录。
