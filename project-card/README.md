# 看着办 · 项目卡

- `看着办-项目卡.png`：3840 × 2160，16:9，可直接提交或放在电脑屏幕展示。
- `看着办-项目卡.html`：图片已内嵌，可单独复制、离线打开，随窗口缩放。
- `index.html`：可编辑排版源文件；修改后运行 `node project-card/render.mjs` 更新交付文件。
- `product-board.png` / `product-full.png`：当前产品界面的截图。

队伍名与 Slogan 按用户提供的“哦，对了”“你看着，AI办，才会顺”填写。上边栏展示成员安昊宇、崔峻赫、吕宗翰，以及用户提供的原始联系二维码。Demo 仅列明本机运行入口；上边栏二维码用于联系团队。

## 内容依据

- 名称与当前运行链：根 `README.md`、`frontend/README.md`，以及当前前后端代码。
- 赛道：`docs/info.md`。
- 起点：初始提交 `64512fb` 只有赛事说明、Agent skill 与仓库基础文件。后续使用已有 DeepSeek Harness 与模型能力开发产品，不宣称自研基础模型或执行框架。
- 当前能力：`frontend/src/components/`、`decision-desk/server/engine.ts`、`reviewer.ts`、`workspace.ts` 等。
- 剩余范围：当前 README 与 `.agent/notes/2026-09-05-backend-runtime-release-audit.md`。较早的 `docs/项目说明.md` 描述历史版本，未将其中逻辑分叉、报告等旧版承诺直接计入当前产品。
- 设计依据：项目 `.agent/skills/design/SKILL.md`（Apple Design），用于字号、信息层级、留白与可读性。

## 截图与验证

截图由当前已构建前端和当前后端在独立临时数据目录、4336 端口运行生成，没有改写 4317 上的任务或模型设置。截图使用项目内置 demo 脚本及规则审查：先产生布局选择，再因“刷新清空”与持久保存冲突而暂停。图中为真实界面及演示状态，已明确标注非真实模型验收。

项目卡已检查 1920 × 1080、1366 × 768 显示比例，并检查文字区溢出、区块重叠和截图加载。未为制作项目卡重跑或宣称通过完整产品验收。

本次修订按用户要求将起点首句改为“起点是三个人类，两种大模型，一份考题”，其后两句不变；原待办区域替换为代码中已有的继续任务、审查重试、任务归档和模型设置功能。项目仍标为可运行原型，截图仍标为内置脚本演示。

## 2026-09-06 更新（本轮修改）

- 品牌图标换成当前的 `BrandMark`（四角开放视框 ＋ 一笔判断线），替掉旧的圆圈中心点；
  依据 `.agent/notes/2026-09-06-brand-mark.md`。
- `product-board.png` 重新截取：本机启动当前后端 + 当前前端构建，读取真实任务
  「帮我构建一个网页版超级马里奥」（run `1844987c`，已完成，17 条决策 / 111 步）的实际状态，
  展开「已验证」栏后截图。视口 1440 × 900、deviceScaleFactor 2、clip 0,0,1440,620，
  与原图尺寸一致（2880 × 1240），可直接替换。截图跑在数据目录的临时副本上，未写入 `frontend/.data`。
  截图说明同步改为「产品实机截图 · 真实任务「网页版超级马里奥」」——这是真实模型运行，不是内置演示脚本。
- 第三栏「任务能继续，过程留得住」改为「在线体验 Demo」，正文为公网演示站地址；
  页脚原来的本机 Demo 地址已删除。
- `render.mjs` 的 `channel:'msedge'` 增加了回退：本机没装 Edge 时用 Playwright 自带的 Chromium。
