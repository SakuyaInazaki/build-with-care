{
  "name": "decision-stream-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev:legacy": "tsx src/server.ts",
    "build:legacy": "tsc -p tsconfig.json && mkdir -p dist/public && cp -R src/public/. dist/public/",
    "start:legacy": "node dist/server.js"
  },
  "deepseekHarness": {
    "version": "0.1.3-alpha.1",
    "status": "optional-adapter-not-installed"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
