# 设置弹窗与项目命名

日期：2026-09-05。

- 用户指出设置界面右侧两角显示成直角。原弹窗本身承担滚动，原生滚动条占用右侧边缘。改为外层统一裁切四角、内层滚动，标题与关闭按钮固定可见；不改变设置行为。
- 用户明确项目名尚未确定，不能直接使用“在场”。新前端暂时使用中性的“工作空间”，移除“在场”品牌及页面标题。旧文档、旧实现的历史名称保留，不将它们视为新品牌确认。
- 候选：定见（人的判断与主见）、有据（决定和结果可追溯）、方寸（过程边界和把握感）。推荐定见，等待用户选择，暂不把候选写成最终项目名。未进行商标或域名可用性核验。
- 顺带修正交互：点击“开始澄清需求”直接发起第一轮 Grill，失败时可重试；确认前仍不能执行。准备中的任务也可查看记录和删除，无需先启动执行。

用户未选择第一轮候选；第二轮提出“主笔、明线、落款、知界、见行”，更推荐主笔或明线，仍待确定。
