# Mario 演示任务保留记录

日期：2026-09-06。

按用户要求，现有网页版 Mario 任务保留在原位置，未更改任务状态、约束、事件或成果。只读检查确认服务为 `unified-work-units-v3`，该任务处于 completed、revision 3，且没有进行中的执行；随后才创建一次本地私有快照。

快照位于被 Git 忽略的 `frontend/.data/demo-backups/mario-web-demo-20260905T164931Z/`。其中 `run/` 完整保留状态文件、产品事件、原始 DSH 事件和 workspace 成果；`manifest.json` 私下记录 run ID、状态、revision、快照时间、文件数、大小及逐文件 SHA-256。没有复制全局 settings、API 配置或密钥。快照目录权限为 `0700`，文件权限为 `0600`。

复制前后的原始目录文件清单与哈希完全一致。另将 `run/` 复制到 `/private/tmp` 下的独立临时目录做恢复演练，逐文件清单、大小及哈希均与原始目录一致，随后删除临时副本。演练没有启动模型或服务，也没有接触线上任务目录。

如以后确需恢复，先停止服务并确认所有任务空闲，从私有 manifest 读取 run ID，验证 `run/` 的逐文件哈希；只有在对应 live run 目录不存在时，才可在同一文件系统的私有 staging 目录复制并再次验哈希，最后原子改名为 `frontend/.data/runs/<run-id>`，再启动服务做只读 bootstrap 核对。现有 live run 存在时不得覆盖。

这份保留副本只是后续演示候选。原始历史保持不变，剩余 8 张缺少针对性语义验证的卡仍然存在；备份与恢复演练不代表三次真实闭环，也不构成冻结首版正式认证。
